version: '3.8'

services:
  app:
    container_name: bflow
    build: 
      context: .
      dockerfile: Dockerfile

    env_file:
      - ../.env

    environment:
      # NOTE: POSTGRES_DB/USER/PASSWORD should match values in db container
        POSTGRES_PASSWORD: postgres
        POSTGRES_USER: postgres
        POSTGRES_DB: bflow
        # Fixed AWS REGION issue as well, uncomment this dummy value if you don't have AWS
        # credentials set up in your .env file to avoid errors related to missing AWS_REGION
        #AWS_REGION: us-east-1

    volumes:
      - ../..:/workspaces:cached
      
    command: sleep infinity

    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    
    networks:
      - asset-network

  db:
    container_name: postgresdb
    image: postgres:16
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      # NOTE: POSTGRES_DB/USER/PASSWORD should match values in app container
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: bflow
    networks:
      - asset-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - asset-network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      retries: 5

networks:
  asset-network:
    driver: bridge

volumes:
  postgres-data: